@using App.Enums;
@model UserModel
@{
    ViewData["Title"] = "Criar usuário";
}

<div class="text-center">
    <h1>Cadastrar usuário</h1>
</div>

<form asp-controller="User" asp-action="Post" method="post">

    <div class="mb-3">
        <label for="name" class="form-label">Nome:</label>
        <input name="name" asp-for="Name" class="form-control" id="name" type="text" placeholder="Digite o nome" />
        @Html.ValidationMessageFor(x => x.Name, "", new { @class = "text-danger" })
    </div>

    <div class="mb-3">
        <label for="email" class="form-label">Email:</label>
        <input name="email" asp-for="Email" class="form-control" id="email" type="email" placeholder="Digite o email" />
        @Html.ValidationMessageFor(x => x.Email, "", new { @class = "text-danger" })
    </div>

    <div class="mb-3">
        <label for="login" class="form-label">Login:</label>
        <input name="login" asp-for="Login" class="form-control" id="login" type="text" placeholder="Digite o login" />
        @Html.ValidationMessageFor(x => x.Login, "", new { @class = "text-danger" })
    </div>

    <div class="mb-3">
        <label for="profile" class="form-label">Perfil:</label>
        <select asp-for="Profile" class="form-control">
            <option value="0">Selecione</option>
            <option value="@ProfileEnum.Admin">Administrador</option>
            <option value="@ProfileEnum.Padrao">Padrão</option>
        </select>
    </div>

    <div class="mb-3">
        <label for="password" class="form-label">Senha:</label>
        <input name="password" asp-for="Password" class="form-control" id="password" type="text" placeholder="Digite a senha" />
        @Html.ValidationMessageFor(x => x.Password, "", new { @class = "text-danger" })
    </div>

    <div class="mb-3">
        <button type="submit" class="btn btn-primary w-100">Adicionar</button>
    </div>

     <div class="mb-3">
        <a  class="btn btn-secondary w-100" asp-controller="User" asp-action="Index" role="button">Voltar</a>
    </div>
</form>